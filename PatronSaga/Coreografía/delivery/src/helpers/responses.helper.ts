/* import { response } from "express";

export class Responses {
  static sentOk(results: any | any[]) {
    res.status(200).json({
      payload: {
        results,
      },
    });
  }

  static sentNotFound(message: string = "Not found") {
    res.status(404).send({
      status: 404,
      message,
    });
  }

  static sentNotAuthenticated(message: string) {}
}
 */
